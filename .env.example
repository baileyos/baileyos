# BaileyOS Configuration
# Copy this file to .env and edit as needed

# Server port (default: 3333)
PORT=3333

# Enable mock mode for testing without real hardware (default: false)
MOCK_MODE=false

# Auth token for dashboard access (generate your own)
# AUTH_TOKEN=your-secure-token-here
